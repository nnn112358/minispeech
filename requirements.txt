# fast-jtts — SqueezeWave 2-stage TTS vocoder
# Python 3.10+; install a CUDA build of torch matching your system separately if training on GPU.
torch>=2.1
torchaudio
numpy
scipy
soundfile
librosa
matplotlib
einops          # used by vendored vocos (discriminators)
onnx
onnxruntime     # CPU inference / benchmarking; use onnxruntime-gpu if desired
# NPU conversion (optional) uses the Pulsar2 docker image (pulsar2:6.0), not a pip package.
